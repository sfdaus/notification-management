apiVersion: v1
kind: Service
metadata:
  name: dashboard-institution-api
  namespace: prakarsa
spec:
  type: ClusterIP
  selector: { app: dashboard-institution-api }
  ports:
    - name: http
      port: 80
      targetPort: 8080
